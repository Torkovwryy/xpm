# Prevent phantom dependencies by strictly enforcing peer dependencies
strict-peer-dependencies=true
auto-install-peers=false

# Lockfile and resolution settings for deterministic builds
lockfile=true
shared-workspace-lockfile=true

# Security: do not execute arbitrary post-install scripts by default globally
# Developers must opt-in per package if needed to avoid RCE vulnerabilities
ignore-scripts=false